langcode: it
status: true
dependencies:
  config:
    - node.type.circolare
id: node.circolare
target_entity_type_id: node
target_bundle: circolare
default_langcode: site_default
language_alterable: true
