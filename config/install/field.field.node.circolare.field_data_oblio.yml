langcode: it
status: true
dependencies:
  config:
    - field.storage.node.field_data_oblio
    - node.type.circolare
  module:
    - datetime
id: node.circolare.field_data_oblio
field_name: field_data_oblio
entity_type: node
bundle: circolare
label: 'Oblio documento'
description: 'Se inserisci una data, in automatico alla data specificata, la pagina del documento non sarà più indicizzabile dai motori di ricerca e non verrà visualizzata negli elenchi dei documenti attivi. Nota che il documento rimarrà comunque visibile nella sezione archivio storico e da coloro che possiedono il link. Se desideri rimuovere il documento dalla pubblicazione togli la spunta dal campo "Pubblicato".'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: datetime
