langcode: it
status: true
dependencies:
  config:
    - field.storage.node.field_abstract
    - node.type.servizio
id: node.servizio.field_abstract
field_name: field_abstract
entity_type: node
bundle: servizio
label: 'Descrizione breve'
description: 'Descrizione sintetica del servizio (si consiglia di non superare i 160 caratteri), usa un linguaggio semplice che possa aiutare qualsiasi utente a identificare con chiarezza il Servizio. Non usare un linguaggio ricco di riferimenti normativi. Questa breve descrizione potrebbe essere anche uno slogan.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string_long
