langcode: it
status: true
dependencies:
  config:
    - field.storage.node.field_tipologia_finanziamento
    - node.type.finanziamento
    - taxonomy.vocabulary.tipologia_finanziamenti
id: node.finanziamento.field_tipologia_finanziamento
field_name: field_tipologia_finanziamento
entity_type: node
bundle: finanziamento
label: 'Tipologia finanziamento'
description: 'Seleziona la tipologia di finanziamento'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      tipologia_finanziamenti: tipologia_finanziamenti
    sort:
      field: name
      direction: asc
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
