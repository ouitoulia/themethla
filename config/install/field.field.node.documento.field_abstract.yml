langcode: it
status: true
dependencies:
  config:
    - field.storage.node.field_abstract
    - node.type.documento
id: node.documento.field_abstract
field_name: field_abstract
entity_type: node
bundle: documento
label: 'Descrizione breve'
description: 'Descrizione sintetica del documento utilizzando un linguaggio semplice che possa aiutare qualsiasi utente a identificare con chiarezza il documento. Si consiglia di non superare i 160 caratteri.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string_long
