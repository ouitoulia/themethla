langcode: it
status: true
dependencies:
  config:
    - node.type.finanziamento
id: node.finanziamento
target_entity_type_id: node
target_bundle: finanziamento
default_langcode: site_default
language_alterable: true
